import Header from '../Header';

export default function PasswordResetSuccessHeader() {
  return (
    <Header
      title="Password reset"
      description="Your password has been reset successfully."
    />
  );
}
